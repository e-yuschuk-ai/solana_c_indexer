# Claude Code instructions

The instructions for this repository live in `AGENTS.md`, so that every agent
tool reads the same file. It is imported here because Claude Code loads
`CLAUDE.md` and would not otherwise see it.

@AGENTS.md
